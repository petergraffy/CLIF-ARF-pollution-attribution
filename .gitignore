# R specific
.Rhistory
.Ruserdata
.DS_Store
*.Rproj
.Rproj.user
*.RData
.Rprofile

# renv (not used — packages installed directly via 00)
/renv

# ignore source CLIF tables
CLIF_*.parquet
CLIF_*.csv
CLIF_*.fst

# ignore the identifier app
CLIF_cohort_identifier*

# ignore rendered reports
*.pdf
*.html

# output files
/output
acs_estimates.csv

# config (site-specific, not shared)
config/config.json
